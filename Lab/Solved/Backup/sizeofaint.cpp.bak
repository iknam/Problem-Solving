#include <map>
#include <set>
#include <stack>
#include <queue>
#include <cmath>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <cstring>

#define	DEBUG(x) cout << '>' << #x << ':' << x << endl;

using namespace std;

int main()
{
	int A[] = {1, 2, 4, 3, 3};
	
	// not work - cout << strlen(A) << endl;
	// A element number
	cout << sizeof(A) << " " << sizeof(int) << " " << sizeof(A) / sizeof(int);

	return 0;
}
