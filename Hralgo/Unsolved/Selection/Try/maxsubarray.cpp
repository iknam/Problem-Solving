#include <iostream>

using namespace std;

int main()
{
	int i, j, T, N;

	cin >> T;

	for (i = 0; i < T; ++i)
	{
		cin >> N;
		
		int *A = new int[N];
		
		for (j = 0; j < N; ++j)
		{
			cin >> A[j];
		}
		
	}
			

    return 0;
}

