#include <iostream>
#include <cstring>
#include <cstdlib>

using namespace std;

int main()
{
	//vector<  > fac;
	int N, i, sub;
	string fac = "";

	cin >> N;

	for (i = N; i > 0; i--)
	{
		//if (fac.length() < 19)
		//{
			fac += to_string(i * (i - 1));
		//}
	}

	cout << fac;
	return 0;
}
