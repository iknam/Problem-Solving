#include <map>
#include <set>
#include <cmath>
#include <stack>
#include <queue>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{	
	vector<int> vi1, vi2;
	int n, num, i;

	cin >> n >> num;

	vi1.resize(num);
	vi2.resize(num);

	for (i = 0; i < num; i++)
	{
		cin >> vi1[i] >> vi2[i];
	}

	return 0;
}
