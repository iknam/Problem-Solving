#include <map>
#include <set>
#include <stack>
#include <queue>
#include <cmath>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>

#define	DEBUG(x) cout << '>' << #x << ':' << x << endl;

// if next line blanks are cover the prev line number then make 2+ way

using namespace std;

int main()
{
	int i, j, M, N;

	cin >> M >> N;
	
	vector<int> vi(N);

	for (i = 0; i < N; i++)
	{
		cin >> vi[i];
	}

	return 0;
}
