#include <map>
#include <set>
#include <cmath>
#include <stack>
#include <queue>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

// if two number is same num, cost deposit only X^2 because wire cost 0

int main()
{
	int N, C, *arr, N_temp;

	cin >> N >> C;

	arr = malloc(N*sizeof(int));

	while(N--)
	{
		cin >> arr[N_temp - N];
	}

	free(arr);

	return 0;
}
