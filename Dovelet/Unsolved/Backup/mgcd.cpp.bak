#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

// odd and even?
// sort after max num is upper bound
// like 19 prime num isn't divided
// when not found solution about never divided like prime,
// it isn't number answer but maybe 1?

int main()
{
	vector<int> v;

	int n, i;
	int cnt = 0;

	cin >> n;

	v.resize(n);

	while(n--) {
		cin >> v[n];
	}

	sort(v.begin(), v.end());
	/*
	for(i = 0; i < v.length(); ++i) {
		if(v[i] % 2 == 0)
			v[i] /= j; 
			cnt++;
	}*/
}


