#include <map>
#include <set>
#include <cmath>
#include <stack>
#include <queue>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <cstdio>

using namespace std;

int main()
{
	int N_rev, N, C, *ar;

	cin >> N >> C;

	ar = (int *)malloc(N*sizeof(int));

	N_rev = N;

	while(N - N_rev)
	{
		scanf("%d" , &ar[N]);
	}

	cout << ar[0];

	return 0;
}
