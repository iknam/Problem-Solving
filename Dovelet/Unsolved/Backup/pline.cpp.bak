#include <map>
#include <set>
#include <stack>
#include <queue>
#include <cmath>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>

#define	DEBUG(x) cout << '>' << #x << ':' << x << endl;

using namespace std;

int main()
{
	int i, j, M, N;

	cin >> M >> N;
	
	int ar[N];

	// vector<int> vi(N);

	for (i = 0; i < N; i++)
	{
		ar[i] = 1;
	}

	cout << ar[N];

	return 0;
}
