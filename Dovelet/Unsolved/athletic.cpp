#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

// use return

void recur(int n)
{
	int max_len = 2 * n - 1;

 	if(n<max_len && ) {
		cout << "o";
	}
	else if(n<max_len && ) {
		cout << "x";
	}

//	recur(n - 1);
	if() {
		cout << "total" << combination << "case(s)";
	}
}
// max_len C n good approach(all sum output line num)
// tail cutting
// o, x assign int_value??
// as it use stl


int main()

	int n;
	
	cin >> n;

	recur(n);
	
	return 0;
}

