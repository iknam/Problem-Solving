#include <map>
#include <set>
#include <cmath>
#include <stack>
#include <queue>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <iomanip>

using namespace std;

int main()
{
	int n, i;

	vector<double> ar;
	double *p, sum = 0;

	//p = ar;

	cin >> n;

	ar.resize(n);

	for(i = 0; i < n; i++)
	{
		cin >> ar[i];
		sum += ar[i];

		cout << fixed;
		cout << setprecision(2) << ar[i] << " ";
	}
	// fixed always?
	cout << endl << "합계 : " << sum << endl << "평균 : " << sum / n;
	return 0;
}
