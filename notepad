-A^-A 는 0

vector 는 N 가능 array 는 N-1 까지 가능 왜?

EOF 키는 control + D (?)

!r $ 로 실행가능 다만 이걸로 실행전에 한번 !r filename 해줘야 하는지 모름 // 안됌ㅈ

더블릿 reverse, hashing, isbn 코드 최적화

왜 str[i] 값이 reverse 를 수행해도 바뀌지 않는지?

char *str = "abcd";

fixed, setprecision 은 한번만 써도 된다

vector 는 v[] 로 사용말고 v.push_back() 으로 사용해보자
그런데 둘의 차이점은 그닥 크게 다른것 같지 않다

리눅스는 터미널에서 아스키 코드를 지원하지 않는다
itoa 정수를 아스키로
많은 데이터를 입력받을땐 scanf로
typedef로 깔끔하게

속도향상
#define push(data)	(stack[++top] = (data))
#define pop()		(stack[top--])
#define is_stack_empty()(top < 0)

run move jump vi notepad default

need to solve codeup 1153

코드업 2016은 그냥 풀면 쉽지만 스택 문제라고 분류되어있음
