Node *head, *tail;

Node *Insert(Node* head, int data)
{
    Node *insert;
    insert = (Node*)malloc(sizeof(Node));

    insert -> data = data;
    tail -> next = head;
    insert -> next = tail;
    
    if (head == '\0')
    {
        return '\0';
    }
    else
    {
        return head;
    }
}
