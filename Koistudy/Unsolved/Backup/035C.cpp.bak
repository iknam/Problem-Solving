#include <map>
#include <set>
#include <cmath>
#include <stack>
#include <queue>
#include <math.h>
#include <string>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

// how to isprime convert number of prime

int main()
{
	int k;

	cin >> k;

	//isprime(k);

	cout << sqrt(k);

	return 0;
}

void isprime(int k)
{
	if (k = 1);
}
