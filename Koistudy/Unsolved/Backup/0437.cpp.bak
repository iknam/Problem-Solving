#include <map>
#include <set>
#include <list>
#include <queue>
#include <stack>
#include <cmath>
#include <vector>
#include <string>
#include <iomanip>
#include <cassert>
#include <sstream>
#include <utility>
#include <iostream>
#include <algorithm>

using namespace std;
// use string processing? maybe it isn't examiner purpose
int recur(int );

int main()
{
	int n;

	cin >> n;

	cout << recur(5);

	return 0;
}

int recur(int n)
{
	if(n < 10)
		return 1;
	else
		return (n /= 10);
}

